= SAÉ 2.03 - README
Lefebvre Romain; Plantard Kylian; Belot Emilien

== Membres de l'équipe
* Romain Lefebvre
* Kylian Plantard
* Emilien Belot

== Présentation du projet
Ce dépôt contient le rendu final de notre SAÉ 2.03 portant sur l'installation de services réseaux (Debian, Git, Forgejo) et la rédaction de documentation technique en AsciiDoc.

== Structure des documents : Pourquoi deux rapports ?
Suite aux consignes de la Semaine 12, nous avons fait le choix de séparer notre rendu en deux types de documents distincts pour plus de clarté. Nous avons donc deux fichiers maîtres à compiler :

* **Le fichier `rapport.adoc` (Le Tutoriel) :** Il raconte notre véritable retour d'expérience sous forme de tutoriel pas-à-pas. Il explique nos choix techniques, les étapes de configuration et les difficultés rencontrées (comme le blocage du port SSH par le NAT ou la gestion des conflits de fusion).
* **Le fichier `rapport_qr.adoc` (Questions / Réponses) :** Il regroupe de manière claire et concise les réponses aux questions théoriques posées dans les différents cahiers des charges, le tout sourcé avec la documentation officielle.

Chacun de ces fichiers maîtres se charge d'inclure (via la directive `include::`) l'ensemble des sous-fichiers correspondants aux différentes semaines.

== Personnalisation du rendu (CSS & PDF)
Pour répondre aux exigences de personnalisation de la dernière semaine, nous avons créé des templates dédiés :

* **Pour le format HTML :** Un fichier `css/style.css` a été créé et lié dans l'en-tête pour moderniser l'affichage (couleurs personnalisées, ombres sur les tableaux, blocs de code type terminal).
* **Pour le format PDF :** Un fichier `mon-theme.yml` a été rédigé pour modifier la police globale, styliser les tableaux et appliquer nos couleurs aux titres via l'attribut `-a pdf-theme` d'Asciidoctor-PDF.

== Procédure de conversion
Notre rapport a été rédigé au format _AsciiDoc_. Pour générer les versions HTML et PDF, nous utilisons un script bash nommé `convertdoc.sh`.

Pour lancer la conversion en local, exécutez la commande suivante dans le terminal :
[source,bash]
----
./convertdoc.sh
----

=== Outils requis pour la compilation locale
Afin de pouvoir compiler ce document, les outils suivants doivent être installés sur la machine :

* **Ruby** (nécessaire pour l'écosystème Asciidoctor)
* **Asciidoctor** : Convertisseur principal (vers HTML)
* **Asciidoctor-pdf** : Convertisseur vers le format PDF
* **Rouge** : Bibliothèque permettant la coloration syntaxique des blocs de code.

Sur les machines de l'IUT, nous avons installé ces outils localement avec la commande suivante :
`gem install --user-install asciidoctor asciidoctor-pdf rouge`

Nous avons ensuite ajouté le chemin des binaires Ruby au `PATH` de notre environnement utilisateur pour rendre les commandes accessibles.